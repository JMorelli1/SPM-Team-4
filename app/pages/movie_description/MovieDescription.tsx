const MovieDescription = ({ movieId }: {movieId: string}) => {
    return (
        <div>
            <h1>Movie Description Page {movieId}</h1>
        </div>
    );
}
export default MovieDescription;